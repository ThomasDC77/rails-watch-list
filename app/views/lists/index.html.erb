<h1>Voici les listes des films</h1>

<% @lists.each do |list| %>
  <%= link_to list.name, list_path(list) %>
<% end %>

<%= link_to "Créer une liste", new_list_path %>
<%# <%= link_to "Allez au bookmarks", list_bookmarks_path %>
